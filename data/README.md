- `data/R` contains the raw data preparation pipeline for the application
- Data structure:
    + `data/raw` holds raw data files
    + `data/intermediate` holds intermediate tables, files, databases, etc.
    + `data/assets` holds data assets to support app